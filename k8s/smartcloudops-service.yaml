apiVersion: v1
kind: Service
metadata:
  name: smartcloudops-main
  namespace: smartcloudops
  labels:
    app: smartcloudops
    component: main
  annotations:
    # Service annotations for load balancing
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
    service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-path: "/health"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-port: "5000"

    # Prometheus service discovery
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
    prometheus.io/path: "/metrics"

spec:
  type: ClusterIP
  selector:
    app: smartcloudops
    component: main

  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP

  - name: metrics
    port: 8000
    targetPort: 8000
    protocol: TCP

  - name: websocket
    port: 8080
    targetPort: 8080
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: smartcloudops-worker
  namespace: smartcloudops
  labels:
    app: smartcloudops
    component: worker
spec:
  type: ClusterIP
  selector:
    app: smartcloudops
    component: worker

  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: smartcloudops-ml-service
  namespace: smartcloudops
  labels:
    app: smartcloudops
    component: ml-service
spec:
  type: ClusterIP
  selector:
    app: smartcloudops
    component: ml-service

  ports:
  - name: http
    port: 80
    targetPort: 5001
    protocol: TCP

  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: smartcloudops
  labels:
    app: postgres
    component: database
spec:
  type: ClusterIP
  selector:
    app: postgres

  ports:
  - name: postgresql
    port: 5432
    targetPort: 5432
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: smartcloudops
  labels:
    app: redis
    component: cache
spec:
  type: ClusterIP
  selector:
    app: redis

  ports:
  - name: redis
    port: 6379
    targetPort: 6379
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: smartcloudops
  labels:
    app: prometheus
    component: monitoring
spec:
  type: ClusterIP
  selector:
    app: prometheus

  ports:
  - name: http
    port: 9090
    targetPort: 9090
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: smartcloudops
  labels:
    app: grafana
    component: dashboard
spec:
  type: ClusterIP
  selector:
    app: grafana

  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: smartcloudops
  labels:
    app: alertmanager
    component: alerting
spec:
  type: ClusterIP
  selector:
    app: alertmanager

  ports:
  - name: http
    port: 9093
    targetPort: 9093
    protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: smartcloudops
  labels:
    app: mlflow
    component: mlops
spec:
  type: ClusterIP
  selector:
    app: mlflow

  ports:
  - name: http
    port: 5001
    targetPort: 5001
    protocol: TCP
