{
  "name": "smartcloudops-ai-backend",
  "version": "1.0.0",
  "description": "Smart CloudOps AI - Backend Application",
  "scripts": {
    "dev:api": "cd . && . venv/bin/activate && FLASK_PORT=8000 FLASK_HOST=127.0.0.1 python -m flask --app app.main run --host=127.0.0.1 --port=8000",
    "build:api": "echo \"Backend build completed (Python Flask)\"",
    "install:all": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt",
    "test:smoke": "curl -sf http://localhost:5000/health && echo \" - Health check passed\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@0xshariq/docker-mcp-server": "^2.0.4",
    "@henkey/postgres-mcp-server": "^1.0.5",
    "@pickstar-2002/redis-mcp": "^1.0.4",
    "grafana-mcp-analyzer": "^2.1.0",
    "prometheus-mcp": "^1.1.3"
  }
}
