name: Test Simple Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-job:
    name: Test Job
    runs-on: ubuntu-latest
    steps:
    - name: Test Step
      run: echo "This is a test workflow"
